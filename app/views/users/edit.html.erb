<%= link_to 'Back', @user, class: 'btn btn-secondary mb-3' %>
<div class="card">
  <div class="card-body">
    <h2 class='card-title mb-4'>
      <span>Editing User</span>
    </h2>

    <%= render 'form', user: @user %>
  </div>
</div>